import { HighlightParticularWordPipe } from './highlight-particular-word.pipe';

describe('HighlightParticularWordPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightParticularWordPipe();
    expect(pipe).toBeTruthy();
  });
});
