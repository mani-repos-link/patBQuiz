import { ClickedWordPipe } from './clicked-word.pipe';

describe('ClickedWordPipe', () => {
  it('create an instance', () => {
    const pipe = new ClickedWordPipe();
    expect(pipe).toBeTruthy();
  });
});
